\section{Real-System Validation}
\label{sec:real-system}

\emph{[PENDING RESULT: sign agreement, Kendall's tau, and
reversal-agreement between simulated and real-vLLM policy rankings, for a
frozen, objectively selected pair of validation cases -- the
largest-effect-size reversal identified in \S\ref{sec:reversals} plus one
stable-ordering control condition. Case selection must be frozen from this
study's own \S\ref{sec:cross-source-rankings}/\S\ref{sec:reversals} results
before any real-vLLM run is executed, and cannot be written until those
results exist.]}

This analysis answers RQ6 (\S\ref{sec:rq}). We do not claim, here or
anywhere in this manuscript, that simulated absolute latency or throughput
values match real-hardware values; the only claim in scope is agreement in
\emph{relative ranking} and \emph{reversal direction} between the simulator
and a real vLLM deployment on the selected cases
(\texttt{docs/REAL\_SYSTEM\_VALIDATION\_PLAN.md}).
